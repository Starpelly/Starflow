using System.Threading;
using System.ComponentModel;

namespace Starflow.Editor.Runtime
{
    internal class GameRuntime
    {
        public static void Run()
        {
            Start();
        }

        private static void Start()
        {
            /*using (var game = new Sandbox.Sandbox())
                game.Run();*/
        }
    }
}
